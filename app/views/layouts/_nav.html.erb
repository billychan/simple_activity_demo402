<nav class="top-bar" data-topbar data-options="is_hover: false">
  <ul class="title-area">
    <li class="name">
      <h1><a href="#">SimpleActivity Demo</a></h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <li><%= link_to "Questions", root_path %></li>
      <li><%= link_to "Users", users_path %></li>
      <li class="divider"></li>
      <li class="has-form">
        <%= link_to "Ask Question", new_question_path, class: 'button' %>
      </li>
      <li class="divider"></li>
      <li>
        <% if current_user %>
          <%= link_to current_user.name, "#" %>
        <% end %>
      </li>
      <li class="has-dropdown">
        <a href="#">Switch users</a>
        <ul class="dropdown">
          <% User.demo_users.each do |user| %>
            <li>
              <%= link_to user.name, switch_session_path(user), method: :post %>
            </li>
          <% end %>
        </ul>
      </li>
    </ul>
  </section>
</nav>
