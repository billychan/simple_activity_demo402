$ ->
  $(document).foundation()
  App.start()


  # $('#answers li.answer .manage').hide()

  # $('#answers li.answer').mouseover ->
  #   $(@).find('.manage').show()
  # .mouseout ->
  #   $(@).find('.manage').hide()
